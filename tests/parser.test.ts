import { describe, test, expect, beforeEach, afterEach } from 'bun:test';

describe('Coookie Parser', () => {

	test('', async () => {

		return new Promise<void>((resolve, reject) => {
const timeout = setTimeout(() => reject(new Error('Parse timeout')), 5000);
		})
	});
});
