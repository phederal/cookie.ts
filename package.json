{
  "name": "cookie.ts",
  "description": "Cookie parser for all formats. Json, Netspace, SetCookie, String.",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:parser": "bun test parser"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}